---
import "../styles/footer.css";
import CookieConsent from "./CookieConsent.astro";
import LegacyShopPanel from "./LegacyShopPanel.astro";

const { showTrust = true } = Astro.props;

const heurekaUrl = "https://obchody.heureka.sk/tonerymaxim-sk/recenze/";

const paymentLogos = [
  { src: "/images/footer-logos/gopay-banner-colorfull.png", alt: "GoPay", width: 100, height: 35 },
  { src: "/images/footer-logos/img (1).png", alt: "Visa", width: 67, height: 23 },
  { src: "/images/footer-logos/img (2).png", alt: "Visa Electron", width: 67, height: 43 },
  { src: "/images/footer-logos/img.png", alt: "Verified by Visa", width: 67, height: 30 },
  { src: "/images/footer-logos/mc_vrt_opt_pos_73_2x.png", alt: "Mastercard", width: 140, height: 115 },
  { src: "/images/footer-logos/ms_vrt_opt_pos_73_2x.png", alt: "Maestro", width: 140, height: 115 },
  { src: "/images/footer-logos/mc_securecode_156px_width.png", alt: "Mastercard SecureCode", width: 156, height: 72 },
];

const shippingLogos = [
  { src: "/images/footer-logos/gls-logo-optimized.webp", alt: "GLS kuriér", width: 220, height: 150 },
  { src: "/images/footer-logos/gls-ptp-optimized.webp", alt: "GLS ParcelShop", width: 300, height: 170 },
  { src: "/images/footer-logos/dpd-logo-optimized.webp", alt: "DPD kuriér", width: 300, height: 133 },
  { src: "/images/footer-logos/dpd-pickup-optimized.webp", alt: "DPD Pickup", width: 240, height: 77 },
];
---

{showTrust && (
  <section class="footer-support-strip" aria-label="Kontakt a dôvera ToneryMaxim">
    <div class="wrap footer-support-inner">
      <div class="footer-support-copy">
        <span>Potrebujete poradiť?</span>
        <strong>Pomôžeme vám vybrať správnu náplň ešte pred objednávkou.</strong>
      </div>
      <div class="footer-support-actions">
        <a href="tel:+421917859206">☎ +421 917 859 206</a>
        <a href="mailto:info@tonerymaxim.sk">✉ info@tonerymaxim.sk</a>
        <small>Po–Pi 9:00–15:00</small>
      </div>
    </div>
  </section>
)}

<section class="mobile-newsletter" aria-label="Newsletter ToneryMaxim">
  <form class="footer-newsletter" data-footer-newsletter>
    <strong>Novinky, akcie a výhody do e-mailu</strong>
    <div><input type="email" name="email" required autocomplete="email" placeholder="vas@email.sk" aria-label="E-mail pre newsletter" /><button type="submit">Prihlásiť</button></div>
    <label><input type="checkbox" required /> <span>Súhlasím so zasielaním marketingových informácií. <a href="/newsletter">Viac o newsletteri</a></span></label>
    <small data-footer-newsletter-message hidden></small>
  </form>
</section>

<footer class="site-footer" id="kontakt">
  <div class="wrap footer-main">
    <div class="footer-brand">
      <a class="footer-logo" href="/" aria-label="ToneryMaxim.sk">
        <img src="/images/tonerymaxim-logo-sharp.png" alt="ToneryMaxim" width="1270" height="222" loading="lazy" decoding="async" />
      </a>
      <p>Slovenský špecialista na tonery, atramentové náplne a komponenty pre tlačiarne.</p>
      <form class="footer-newsletter" data-footer-newsletter>
        <strong>Novinky, akcie a výhody do e-mailu</strong>
        <div><input type="email" name="email" required autocomplete="email" placeholder="vas@email.sk" aria-label="E-mail pre newsletter" /><button type="submit">Prihlásiť</button></div>
        <label><input type="checkbox" required /> <span>Súhlasím so zasielaním marketingových informácií. <a href="/newsletter">Viac</a></span></label>
        <small data-footer-newsletter-message hidden></small>
      </form>
    </div>

    <nav class="footer-col" aria-label="Zákaznícka podpora">
      <h3>Zákaznícka podpora</h3>
      <a href="/kontakt">Kontakt</a>
      <a href="/faq">FAQ</a>
      <a href="/poradna">Odborná poradňa</a>
      <a href="/doprava-a-platba">Doprava</a>
      <a href="/reklamacie">Reklamácie</a>
      <a href="/reklamacia-online">Reklamácia online</a>
      <a href="/odstupenie-od-zmluvy">Odstúpenie od zmluvy</a>
      <a href="/obchodne-podmienky">Obchodné podmienky</a>
    </nav>

    <nav class="footer-col" aria-label="Sortiment">
      <h3>Sortiment</h3>
      <a href="/tonery">Tonery</a>
      <a href="/atramentove-naplne">Atramentové náplne</a>
      <a href="/opticke-valce">Optické valce</a>
      <a href="/komponenty-do-tlaciarni">Ostatné komponenty</a>
    </nav>

    <nav class="footer-col" aria-label="Firma">
      <h3>Firma</h3>
      <a href="/o-nas">O nás</a>
      <a href="/ako-overujeme-kompatibilitu">Ako overujeme kompatibilitu</a>
      <a href="/kontakt">Kontakt</a>
      <a href="/toner-bez-starosti">Toner bez starostí</a>
      <a href="/spatny-odber-tonerov">Spätný odber tonerov</a>
      <a href="/vernostny-program">Vernostný program</a>
      <a href="/newsletter">Newsletter</a>
      <a href="/ochrana-osobnych-udajov">GDPR</a>
      <a href="/partneri-a-tretie-strany">Partneri a tretie strany</a>
      <a href="/cookies">Informácie o cookies</a>
      <a href="/cookies#nastavenia" data-open-cookie-settings>Nastavenia cookies</a>
    </nav>

    <div class="footer-contact-box">
      <h3>Kontakt</h3>
      <a href="tel:+421917859206">☎ +421 917 859 206</a>
      <a href="mailto:info@tonerymaxim.sk">✉ info@tonerymaxim.sk</a>
      <p>Po–Pi 9:00–15:00</p>
      <p class="footer-company-data">Roman Babčan INkarus<br />Tajov 265, 976 34 Tajov<br />IČO: 37 328 344 · DIČ: 1020059920 · IČ DPH: SK1020059920</p>
      <a class="footer-contact-rating" href={heurekaUrl} target="_blank" rel="noopener noreferrer" aria-label="Otvoriť recenzie ToneryMaxim na Heureke">
        <span class="footer-heureka-stars" data-heureka-stars aria-hidden="true">★★★★★</span>
        <span class="footer-heureka-details">
          <strong><span data-heureka-rating>4,9</span>/5</strong>
          <small><b data-heureka-count>1 326</b> recenzií</small>
          <small><b data-heureka-recommendation>100 %</b> odporúča</small>
        </span>
      </a>
    </div>
  </div>

  <div class="wrap footer-trust-row" aria-label="Platby a doprava">
    <div class="footer-methods footer-payment-methods">
      <strong>Platby</strong>
      <div class="footer-logo-list footer-logo-list--payments">
        {paymentLogos.map((logo) => (
          <span class="footer-logo-tile">
            <img src={logo.src} alt={logo.alt} width={logo.width} height={logo.height} loading="lazy" decoding="async" />
          </span>
        ))}
      </div>
    </div>

    <div class="footer-methods footer-shipping-methods">
      <strong>Doprava</strong>
      <div class="footer-logo-list footer-logo-list--shipping">
        {shippingLogos.map((logo) => (
          <span class="footer-logo-tile footer-logo-tile--shipping">
            <img src={logo.src} alt={logo.alt} width={logo.width} height={logo.height} loading="lazy" decoding="async" />
          </span>
        ))}
      </div>
    </div>
  </div>

  <div class="wrap footer-bottom">
    <span>© 2026 ToneryMaxim.sk</span>
    <div>
      <a href="/ochrana-osobnych-udajov">GDPR</a>
      <a href="/cookies">Cookies</a>
      <a href="/obchodne-podmienky">Obchodné podmienky</a>
      <a href="/reklamacie">Reklamácie</a>
      <a href="/reklamacia-online">Reklamácia online</a>
      <a href="/odstupenie-od-zmluvy">Odstúpenie</a>
    </div>
  </div>
</footer>

<CookieConsent />
<LegacyShopPanel />

<script>
  function formatSkNumber(value: unknown) {
    const number = Number(value || 0);
    if (!Number.isFinite(number) || number <= 0) return "";
    return new Intl.NumberFormat("sk-SK").format(number);
  }

  function formatSkRating(value: unknown) {
    const number = Number(value || 0);
    if (!Number.isFinite(number) || number <= 0) return "";
    return number.toLocaleString("sk-SK", { minimumFractionDigits: 1, maximumFractionDigits: 1 });
  }

  function renderStars(rating: unknown) {
    const value = Number(rating || 0);
    const rounded = Math.max(0, Math.min(5, Math.round(value)));
    return "★".repeat(rounded || 5) + "☆".repeat(Math.max(0, 5 - (rounded || 5)));
  }

  function updateFooterHeureka(data: any) {
    if (data?.ok !== true) return;

    const reviews = Array.isArray(data?.reviews) ? data.reviews : [];
    const reviewCards = Array.from(document.querySelectorAll<HTMLElement>("[data-heureka-review-card]"));

    reviews.slice(0, reviewCards.length).forEach((review: any, index: number) => {
      const card = reviewCards[index];
      const textNode = card?.querySelector<HTMLElement>("[data-heureka-review-text]");
      const authorNode = card?.querySelector<HTMLElement>("[data-heureka-review-author]");
      const reviewText = String(review?.summary || review?.pros || "").trim();
      const author = String(review?.author || "").trim();
      if (textNode && reviewText) textNode.textContent = `„${reviewText}“`;
      if (authorNode && author) authorNode.textContent = author;
    });

    const rating = Number(data?.averageRating || 0);
    const reviewCount = Number(data?.reviewCount || 0);
    if (rating <= 0 || reviewCount <= 0) return;
    const ratingText = formatSkRating(rating);
    const countText = formatSkNumber(reviewCount);
    const stars = renderStars(rating);

    document.querySelectorAll("[data-heureka-count], [data-heureka-bottom-count]").forEach((node) => {
      node.textContent = countText;
    });
    document.querySelectorAll("[data-heureka-mini-count]").forEach((node) => {
      node.textContent = `${countText} recenzií`;
    });

    document.querySelectorAll("[data-heureka-rating], [data-heureka-mini-score]").forEach((node) => {
      node.textContent = node.hasAttribute("data-heureka-mini-score") ? `${ratingText}/5` : ratingText;
    });

    document.querySelectorAll("[data-heureka-stars]").forEach((node) => {
      node.textContent = stars;
    });
    if (Number(data?.recommendationPercent || 0) > 0) {
      document.querySelectorAll("[data-heureka-recommendation]").forEach((node) => {
        node.textContent = `${Math.round(Number(data.recommendationPercent))} %`;
      });
    }
  }

  async function loadFooterHeurekaStats() {
    const cacheKey = "tm_heureka_stats_v1";
    try {
      const cached = JSON.parse(sessionStorage.getItem(cacheKey) || "null");
      if (cached && Date.now() < Number(cached.expiresAt || 0) && cached.data) {
        updateFooterHeureka(cached.data);
        return;
      }
      const response = await fetch("/api/heureka-reviews", {
        headers: { Accept: "application/json" },
        cache: "default",
      });
      const data = await response.json();
      updateFooterHeureka(data);
      try { sessionStorage.setItem(cacheKey, JSON.stringify({ data, expiresAt: Date.now() + 1800000 })); } catch {}
    } catch (error) {
      // Zostanú zobrazené posledné overené hodnoty a recenzie uložené v HTML.
    }
  }

  loadFooterHeurekaStats();
</script>

<script>
  document.querySelectorAll<HTMLFormElement>("[data-footer-newsletter]").forEach((newsletterForm) => newsletterForm.addEventListener("submit", async (event) => {
    event.preventDefault(); const form=event.currentTarget as HTMLFormElement; const button=form.querySelector<HTMLButtonElement>("button")!; const message=form.querySelector<HTMLElement>("[data-footer-newsletter-message]")!; button.disabled=true; message.hidden=true;
    try { const email=String(new FormData(form).get("email")||""); const response=await fetch("/api/newsletter/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,source:"footer"})}); const data=await response.json(); message.textContent=data.message||data.error||"Hotovo."; message.className=response.ok?"is-success":"is-error"; message.hidden=false; if(response.ok)form.reset(); } catch { message.textContent="Prihlásenie sa nepodarilo. Skúste to neskôr."; message.className="is-error"; message.hidden=false; } finally { button.disabled=false; }
  }));
</script>
